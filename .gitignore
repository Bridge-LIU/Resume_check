# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — 実ファイルは除外、テンプレ（*.example）だけ追跡許可
.env
.env.*
!.env.example
!.env.*.example
!.env*.example

# 機密：PII（履歴書・面談内容・評価）＋ APIキー含む設定。ローカル保存、git には上げない
# 例外として配布 pkg に同梱する既定マスタ（5 役割 + 評価条件）のみ追跡する。
# gitignore の仕様上、親ディレクトリを exclude すると子の再包含が効かないため、
# /data/* + /data/master/roles/* で「内容」を除外しつつ、必要なファイル/ディレクトリ
# を明示的に unmask する構造にする。開発者ローカルで書き換えた内容が誤って push
# されないよう git add 時に注意。
/data/*
!/data/master/
/data/master/*
!/data/master/eval_criteria.json
!/data/master/roles/
/data/master/roles/*
!/data/master/roles/Dev.json
!/data/master/roles/ITSupport.json
!/data/master/roles/NW.json
!/data/master/roles/PMO.json
!/data/master/roles/Server.json

# 履歴書抽出のローカル検証用サンプル（scripts/dev/gen-resume-samples.mjs 自動生成）
/test-files/

# 開発者専用スクリプト（seed / claude-Nsplit / mockup 資材生成など、配布に含めない）
/scripts/dev/

# superpowers brainstorming セッション（ローカル一時ファイル）
/.superpowers/

# ローカルプレビュー用の PNG / HTML / スクリプト（画面ショットや mockup など）
/.preview/
.preview*

# auto-shutdown で lib/heartbeat.ts が書き出す終了理由（start.bat が読取・削除）
# 診断モードではタイムスタンプ付きでリネームして残す (`.auto-shutdown.reason.YYYYMMDD_HHMMSS`)
.auto-shutdown.reason
.auto-shutdown.reason.*

# 診断モード：app/api/heartbeat/route.ts が POST/GET 到達を追記する
# module state と独立に到達数を計測して pingLog 0 の原因を切り分ける
.heartbeat-arrivals.log
.heartbeat-arrivals.log.*

# 配布 ZIP 実験置き場（pkg-v*, app-v*.zip 等の展開済みビルド成果物、数百 MB）
/test-release/

# start.bat portable Node.js（初回起動時に自動展開）
/node-portable/
/node-portable.zip

# 開発中の preview mockup（配布対象外、コミット前提としない）
/app/preview-*/

# 社内資料
**/*_設計書.md
**/*_実装計画.md
/docs/
/AGENTS.md
/CLAUDE.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
