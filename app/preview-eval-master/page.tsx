import { EvalMasterVariants } from "./_components/EvalMasterVariants";

export const metadata = {
  title: "評価条件マスタ UI 案 x10 — プレビュー",
};

export default function Page() {
  return <EvalMasterVariants />;
}
